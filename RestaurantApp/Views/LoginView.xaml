<UserControl x:Class="RestaurantApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Border Width="400" Height="400" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="White" CornerRadius="10" BorderBrush="#FFE0E0E0" BorderThickness="1">
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Text="Login to Your Account" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <!-- Email -->
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="10" FontSize="14"/>
                </StackPanel>

                <!-- Password -->
                <StackPanel Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="PasswordBox" Padding="10" FontSize="14"/>
                </StackPanel>

                <!-- Error Message -->
                <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" 
                           Margin="0,0,0,15" TextWrapping="Wrap"/>

                <!-- Login Button -->
                <Button Grid.Row="4" Content="Login" Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        Background="#FF2196F3" Foreground="White" BorderThickness="0"
                        Padding="0,12" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <!-- Register Link -->
                <TextBlock Grid.Row="5" HorizontalAlignment="Center">
                    <Run Text="Don't have an account?"/>
                    <Hyperlink Command="{Binding NavigateToRegisterCommand}">Register here</Hyperlink>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>