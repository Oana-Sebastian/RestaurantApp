<UserControl x:Class="RestaurantApp.Views.LowStockItems"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
             <!--xmlns:vm="clr-namespace:RestaurantApp.ViewModels"-->

    <!--<UserControl.DataContext>
        <vm:LowStockItemsVM/>
    </UserControl.DataContext>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       Text="Low Stock Items" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Threshold:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding StockThreshold, UpdateSourceTrigger=PropertyChanged}" 
                         Width="50" 
                         VerticalAlignment="Center" 
                         Margin="0,0,10,0"/>
                <Button Content="Refresh" 
                        Command="{Binding RefreshCommand}" 
                        Padding="10,5"/>
            </StackPanel>
        </Grid>

        <!-- Low Stock Items List -->
        <DataGrid Grid.Row="1" 
                  Margin="10" 
                  ItemsSource="{Binding LowStockItems}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  AlternatingRowBackground="AliceBlue">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Current Stock" Binding="{Binding TotalQuantity}" Width="Auto"/>
                <DataGridTextColumn Header="Portion Size" Binding="{Binding PortionSize}" Width="Auto"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="Auto"/>
                <DataGridTemplateColumn Header="Status" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="5,2" Margin="2">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TotalQuantity, Converter={StaticResource StockLevelConverter}}" Value="Critical">
                                                <Setter Property="Background" Value="#FFDDDD"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TotalQuantity, Converter={StaticResource StockLevelConverter}}" Value="Low">
                                                <Setter Property="Background" Value="#FFFFDD"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TotalQuantity, Converter={StaticResource StockLevelConverter}}" Value="Critical">
                                                    <Setter Property="Text" Value="Critical"/>
                                                    <Setter Property="Foreground" Value="DarkRed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TotalQuantity, Converter={StaticResource StockLevelConverter}}" Value="Low">
                                                    <Setter Property="Text" Value="Low"/>
                                                    <Setter Property="Foreground" Value="DarkOrange"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <TextBlock Text="{Binding StatusMessage}"/>
        </StatusBar>
    </Grid>
</UserControl>