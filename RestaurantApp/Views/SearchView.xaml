<UserControl x:Class="RestaurantApp.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantApp.Views"
             xmlns:converters="clr-namespace:RestaurantApp.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Search Menu" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Search Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Input -->
            <TextBox Grid.Column="0" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="8" FontSize="14" Margin="0,0,10,0"/>

            <!-- Search Type Selector -->
            <ComboBox Grid.Column="1" ItemsSource="{Binding SearchTypes}" 
                      SelectedItem="{Binding SelectedSearchType}" 
                      Width="150" Padding="8" Margin="0,0,10,0"/>

            <!-- Include/Exclude Selector -->
            <ComboBox Grid.Column="2" ItemsSource="{Binding SearchModes}" 
                      SelectedItem="{Binding SelectedSearchMode}" 
                      Width="120" Padding="8" Margin="0,0,10,0"/>

            <!-- Search Button -->
            <Button Grid.Column="3" Content="Search" Command="{Binding SearchCommand}" 
                    Padding="15,8" Background="#FF2196F3" Foreground="White" BorderThickness="0"/>
        </Grid>

        <!-- Results -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding SearchResults}">
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" Margin="0,20,0,10"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="5" 
                                Margin="0,0,0,15" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Item Image -->
                                <Border Grid.Column="0" Width="80" Height="80" Background="#FFEEEEEE" CornerRadius="3" Margin="0,0,15,0">
                                    <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                </Border>

                                <!-- Item Details -->
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding PortionQuantity, StringFormat={}Portion: {0}g}" Margin="0,5,0,0"/>

                                    <!-- Allergens -->
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0" 
                                                Visibility="{Binding HasAllergens, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="Allergens: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding AllergensList}"/>
                                    </StackPanel>

                                    <!-- Availability -->
                                    <TextBlock Text="INDISPONIBIL" Foreground="Red" FontWeight="Bold" Margin="0,5,0,0"
                                               Visibility="{Binding IsAvailable, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                </StackPanel>

                                <!-- Price and Button -->
                                <StackPanel Grid.Column="2" MinWidth="100">
                                    <TextBlock Text="{Binding Price, StringFormat={}{0:C}}" FontWeight="Bold" 
                                               HorizontalAlignment="Right" Margin="0,0,0,10"/>

                                    <Button Content="Add to Order" 
                                            Command="{Binding DataContext.AddToOrderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                            CommandParameter="{Binding}"
                                            Padding="8,5" Background="#FF4CAF50" Foreground="White" BorderThickness="0"
                                            Visibility="{Binding IsAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>